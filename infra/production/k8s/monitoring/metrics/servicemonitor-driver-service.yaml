apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: driver-service
  namespace: monitoring
  labels:
    app: driver-service
    release: monitoring
spec:
  selector:
    matchLabels:
      app: driver-service
      component: metrics
  namespaceSelector:
    matchNames:
    - prod
  endpoints:
  - port: metrics
    interval: 15s
    path: /metrics
    scheme: http
